import React from 'react'
import ReactDOM from 'react-dom/client'
import BoredView from './view/BoredView'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BoredView />
  </React.StrictMode>
)